spring:
  datasource:
    url: jdbc:h2:mem:cashback_test;DB_CLOSE_DELAY=-1
  jpa:
    hibernate:
      ddl-auto: create-drop
  h2:
    console:
      enabled: false

app:
  kafka:
    out-topic: cashback.applied.test
